{
  "name": "recall",
  "description": "Search long-term memory for facts matching a keyword or phrase",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Keyword or phrase to search for in memory"
      }
    },
    "required": ["query"]
  },
  "secrets": [],
  "timeout_ms": 3000,
  "handler": "handler.js",
  "runtime": "node"
}
