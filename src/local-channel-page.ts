import { WEB_MARKDOWN_SCRIPT } from "./web-markdown.ts";

export const LOCAL_CHANNEL_PAGE = `<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>NewsTeam — Local Chat</title>
<link rel="icon" type="image/svg+xml" href="/favicon.ico">
<style>
:root { --bg:#0d1117;--surface:#161b22;--surface2:#21262d;--border:#30363d;--text:#e6edf3;--muted:#8b949e;--accent:#58a6ff;--green:#3fb950;--red:#f85149;--user:#1f6feb33;--digest:#d2992230; }
[data-theme="light"] { --bg:#f6f8fa;--surface:#fff;--surface2:#f0f3f6;--border:#d0d7de;--text:#1f2328;--muted:#656d76;--accent:#0969da;--green:#1a7f37;--red:#cf222e;--user:#ddf4ff;--digest:#fff8c5; }
*{box-sizing:border-box}html,body{height:100%;margin:0}body{font:14px/1.5 system-ui,-apple-system,sans-serif;background:var(--bg);color:var(--text);overflow:hidden}button,textarea{font:inherit}.app{height:100%;display:grid;grid-template-columns:260px 1fr}.rail{min-height:0;background:var(--surface);border-right:1px solid var(--border);padding:18px 12px;overflow:auto}.brand{color:var(--accent);font-weight:700;font-size:16px;padding:0 8px 16px}.agent-label{color:var(--muted);font-size:11px;text-transform:uppercase;letter-spacing:.08em;padding:14px 8px 5px}.channel{width:100%;display:flex;align-items:center;gap:7px;border:0;background:transparent;color:var(--muted);padding:8px;border-radius:6px;text-align:left;cursor:pointer}.channel:hover,.channel.active{background:var(--surface2);color:var(--text)}.channel-name{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.badge{font-size:9px;color:var(--accent);border:1px solid var(--border);border-radius:8px;padding:0 4px}.unread{margin-left:auto;width:7px;height:7px;border-radius:50%;background:var(--accent)}.profile{position:relative;display:inline-grid;place-items:center;overflow:hidden;border:1px solid var(--border);border-radius:50%;background:var(--surface2);color:var(--accent);font-size:11px;font-weight:700;line-height:1}.profile img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover}.profile-fallback{display:block}.rail-avatar{width:24px;height:24px;flex:0 0 24px}.header-profile{display:flex;flex:0 0 auto}.header-avatar{width:34px;height:34px}.message-avatar{width:32px;height:32px;margin-top:1px;flex:0 0 32px}.main{min-width:0;min-height:0;height:100%;overflow:hidden;display:grid;grid-template-rows:auto minmax(0,1fr) auto}.header{height:60px;border-bottom:1px solid var(--border);display:flex;align-items:center;padding:0 20px;gap:12px}.header-title{font-weight:650;flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis}.nav,.small-button{color:var(--muted);background:var(--surface);border:1px solid var(--border);border-radius:6px;padding:6px 9px;text-decoration:none;cursor:pointer}.nav:hover,.small-button:hover{color:var(--accent);border-color:var(--accent)}.stream{min-height:0;overflow-y:auto;overscroll-behavior:contain;padding:24px max(24px,calc((100% - 850px)/2));scroll-behavior:smooth}.message{max-width:780px;margin:0 0 18px}.message.user{margin-left:auto;background:var(--user);padding:10px 13px;border-radius:12px 12px 3px 12px;white-space:pre-wrap;overflow-wrap:anywhere}.message.agent{display:grid;grid-template-columns:32px minmax(0,1fr);gap:10px}.message.system{border-left:2px solid var(--border);padding-left:14px}.message-content{min-width:0}.message.digest,.message.synthesis{background:var(--digest);border:1px solid var(--border);border-left:3px solid #d29922;border-radius:8px;padding:16px}.message.command{font-family:'SF Mono',Consolas,monospace;font-size:12px}.message.error{color:var(--red)}.meta{color:var(--muted);font-size:10px;margin-bottom:4px}.markdown p{margin:.45em 0}.markdown h1,.markdown h2,.markdown h3,.markdown h4{margin:1em 0 .35em;line-height:1.25}.markdown h1{font-size:1.35em}.markdown h2{font-size:1.2em}.markdown h3,.markdown h4{font-size:1.05em}.markdown ul,.markdown ol{padding-left:22px}.markdown code{font-family:'SF Mono',Consolas,monospace;background:var(--surface2);padding:1px 4px;border-radius:4px}.markdown pre{background:var(--surface2);border:1px solid var(--border);border-radius:6px;padding:12px;overflow:auto}.markdown pre code{padding:0;background:none}.markdown blockquote{margin:8px 0;padding-left:12px;border-left:3px solid var(--border);color:var(--muted)}.markdown a{color:var(--accent)}.typing{display:flex;align-items:center;gap:10px;color:var(--muted);font-size:12px;padding:0 0 14px}.typing-copy span{animation:pulse 1.1s infinite}.typing-copy span:nth-child(2){animation-delay:.15s}.typing-copy span:nth-child(3){animation-delay:.3s}@keyframes pulse{0%,60%,100%{opacity:.25}30%{opacity:1}}.confirmation-card{border:1px solid var(--accent);border-radius:8px;padding:14px;background:var(--surface)}.confirmation-actions{display:flex;gap:8px;margin-top:10px}.approve{color:var(--green)}.deny{color:var(--red)}.composer-wrap{border-top:1px solid var(--border);padding:14px max(20px,calc((100% - 850px)/2));background:var(--bg)}.reset-confirm{display:none;align-items:center;gap:8px;color:var(--muted);font-size:12px;margin-bottom:8px}.reset-confirm.show{display:flex}.composer{display:flex;gap:9px;align-items:flex-end}.composer textarea{resize:none;min-height:46px;max-height:180px;flex:1;background:var(--surface);color:var(--text);border:1px solid var(--border);border-radius:8px;padding:11px 12px;outline:none}.composer textarea:focus{border-color:var(--accent)}.send{height:46px;background:var(--accent);color:#fff;border:0;border-radius:8px;padding:0 17px;cursor:pointer;font-weight:650}.send:disabled{opacity:.55}.composer-status{min-height:18px;color:var(--muted);font-size:11px;padding-top:4px}.composer-status.error{color:var(--red)}.empty{color:var(--muted);text-align:center;margin-top:20vh}.theme{width:34px}.mobile-channels{display:none}
@media(max-width:720px){.app{grid-template-columns:1fr}.rail{display:none;position:fixed;inset:0 25% 0 0;z-index:3}.rail.open{display:block}.mobile-channels{display:block}.stream{padding:18px 14px}.composer-wrap{padding:10px}.header{padding:0 12px}}
</style>
</head>
<body><div class="app">
<aside class="rail" id="rail"><div class="brand">NewsTeam</div><div id="channels">Loading channels…</div></aside>
<main class="main"><header class="header"><button class="small-button mobile-channels" id="mobile-channels">☰</button><div class="header-profile" id="header-profile"></div><div class="header-title" id="header-title">Local chat</div><button class="small-button" id="new-button">New conversation</button><a class="nav" href="/">Mission Control</a><button class="small-button theme" id="theme-button" title="Toggle theme">◐</button></header>
<section class="stream" id="stream"><div class="empty">Select a channel to begin.</div></section>
<div class="composer-wrap"><div class="reset-confirm" id="reset-confirm">Clear this agent's current conversation?<button class="small-button approve" id="reset-yes">Clear</button><button class="small-button" id="reset-no">Cancel</button></div><div class="composer"><textarea id="composer" rows="1" placeholder="Message your analyst…"></textarea><button class="send" id="send">Send</button></div><div class="composer-status" id="composer-status"></div></div></main>
</div><script>
${WEB_MARKDOWN_SCRIPT}
var state={channels:[],active:null,seen:new Set(),unread:new Set(),typing:new Set(),confirmations:new Map(),pendingEvents:new Map()};
var $=function(id){return document.getElementById(id)};
function setTheme(theme){document.documentElement.dataset.theme=theme;localStorage.setItem('newsteam-theme',theme)}
setTheme(localStorage.getItem('newsteam-theme')||'dark');
$('theme-button').onclick=function(){setTheme(document.documentElement.dataset.theme==='dark'?'light':'dark')};
function groupChannels(){var groups={};state.channels.forEach(function(c){(groups[c.agent_id]||(groups[c.agent_id]=[])).push(c)});return groups}
function profileInitial(agentId){return((agentId||'?').trim().charAt(0)||'?').toUpperCase()}
function profileElement(channel,className){var avatar=document.createElement('span');avatar.className='profile '+className;var fallback=document.createElement('span');fallback.className='profile-fallback';fallback.textContent=profileInitial(channel.agent_id);fallback.setAttribute('aria-hidden','true');avatar.appendChild(fallback);if(channel.profile_image_url){var image=document.createElement('img');image.src=channel.profile_image_url;image.alt=channel.agent_id+' profile image';image.loading='lazy';image.decoding='async';image.onerror=function(){image.remove()};avatar.appendChild(image)}return avatar}
function renderChannels(){var root=$('channels');root.textContent='';Object.entries(groupChannels()).forEach(function(entry){var label=document.createElement('div');label.className='agent-label';label.textContent=entry[0];root.appendChild(label);entry[1].forEach(function(channel){var button=document.createElement('button');button.className='channel'+(state.active&&state.active.channel_id===channel.channel_id?' active':'');button.dataset.channel=channel.channel_id;var name=document.createElement('span');name.className='channel-name';name.textContent=channel.channel_id;button.append(profileElement(channel,'rail-avatar'),name);if(channel.is_feed_channel){var badge=document.createElement('span');badge.className='badge';badge.textContent='FEED';button.appendChild(badge)}if(state.unread.has(channel.channel_id)){var dot=document.createElement('span');dot.className='unread';button.appendChild(dot)}button.onclick=function(){selectChannel(channel)};root.appendChild(button)})})}
function scrollBottom(){var stream=$('stream');stream.scrollTop=stream.scrollHeight}
function messageElement(message){var element=document.createElement('article');element.className='message '+message.role+' '+message.kind;element.dataset.id=message.id;var meta=document.createElement('div');meta.className='meta';meta.textContent=(message.role==='agent'&&state.active?state.active.agent_id:message.role)+' · '+new Date(message.ts).toLocaleString();var body=document.createElement('div');if(message.role==='user'){body.textContent=message.text}else{body.className='markdown';body.innerHTML=renderMarkdown(message.text)}if(message.role==='agent'&&state.active){var content=document.createElement('div');content.className='message-content';content.append(meta,body);element.append(profileElement(state.active,'message-avatar'),content)}else{element.append(meta,body)}if(message.kind==='confirmation')element.dataset.confirmPreview=message.text;return element}
function appendMessage(message,shouldScroll){if(state.seen.has(message.id))return;state.seen.add(message.id);if(!state.active||message.channel_id!==state.active.channel_id){state.unread.add(message.channel_id);renderChannels();return}var empty=$('stream').querySelector('.empty');if(empty)empty.remove();$('stream').appendChild(messageElement(message));if(shouldScroll)scrollBottom()}
async function selectChannel(channel){state.active=channel;state.unread.delete(channel.channel_id);state.seen.clear();renderChannels();$('header-profile').replaceChildren(profileElement(channel,'header-avatar'));$('header-title').textContent=channel.agent_id+' / #'+channel.channel_id;$('stream').innerHTML='<div class="empty">Loading history…</div>';var response=await fetch('/api/chat/history?channel='+encodeURIComponent(channel.channel_id)+'&n=100');var messages=await response.json();$('stream').textContent='';messages.forEach(function(message){appendMessage(message,false)});if(!messages.length)$('stream').innerHTML='<div class="empty">No messages yet. Say hello.</div>';state.pendingEvents.forEach(function(event){if(event.channel_id===channel.channel_id)confirmationCard(event)});renderTyping();scrollBottom();$('composer').focus();$('rail').classList.remove('open')}
function renderTyping(){var old=$('typing-row');if(old)old.remove();if(!state.active||!state.typing.has(state.active.channel_id))return;var row=document.createElement('div');row.id='typing-row';row.className='typing';var copy=document.createElement('span');copy.className='typing-copy';copy.innerHTML=escapeMarkdownHtml(state.active.agent_id)+' is thinking<span>.</span><span>.</span><span>.</span>';row.append(profileElement(state.active,'message-avatar'),copy);$('stream').appendChild(row);scrollBottom()}
function confirmationCard(event){state.pendingEvents.set(event.confirmation_id,event);if(!state.active||event.channel_id!==state.active.channel_id)return;if(state.confirmations.has(event.confirmation_id))return;var candidates=Array.from($('stream').querySelectorAll('[data-confirm-preview]'));var target=candidates.reverse().find(function(node){return node.dataset.confirmPreview===event.preview&&!node.dataset.confirmationId});if(!target){target=document.createElement('article');target.className='message system confirmation';$('stream').appendChild(target)}target.dataset.confirmationId=event.confirmation_id;target.textContent='';var card=document.createElement('div');card.className='confirmation-card';var preview=document.createElement('div');preview.className='markdown';preview.innerHTML=renderMarkdown(event.preview);var countdown=document.createElement('div');countdown.className='meta';var actions=document.createElement('div');actions.className='confirmation-actions';var approve=document.createElement('button');approve.className='small-button approve';approve.textContent='Approve';var deny=document.createElement('button');deny.className='small-button deny';deny.textContent='Deny';actions.append(approve,deny);card.append(preview,countdown,actions);target.appendChild(card);var update=function(){var seconds=Math.max(0,Math.ceil((new Date(event.expires_at).getTime()-Date.now())/1000));countdown.textContent='Expires in '+seconds+'s';if(!seconds){approve.disabled=true;deny.disabled=true}};update();var timer=setInterval(update,1000);state.confirmations.set(event.confirmation_id,{approve:approve,deny:deny,timer:timer});var resolve=function(value){approve.disabled=true;deny.disabled=true;fetch('/api/chat/confirm',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({confirmation_id:event.confirmation_id,approve:value})})};approve.onclick=function(){resolve(true)};deny.onclick=function(){resolve(false)};scrollBottom()}
function resolvedConfirmation(event){state.pendingEvents.delete(event.confirmation_id);var pending=state.confirmations.get(event.confirmation_id);if(!pending)return;clearInterval(pending.timer);pending.approve.disabled=true;pending.deny.disabled=true;state.confirmations.delete(event.confirmation_id)}
async function sendText(text){if(!state.active)return;var response=await fetch('/api/chat/message',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({channel_id:state.active.channel_id,text:text})});var result=await response.json();if(response.status===202){$('composer').value='';$('composer-status').textContent=result.result==='queued'?'Queued — your analyst will answer after the current turn.':'';$('composer-status').className='composer-status'}else{$('composer-status').textContent=result.result==='busy'?'One message is already queued. Try again when it finishes.':result.result==='rate_limited'?'Please wait a moment before sending again.':(result.error||'Message was not accepted.');$('composer-status').className='composer-status error'}}
$('send').onclick=async function(){var text=$('composer').value.trim();if(!text)return;this.disabled=true;try{await sendText(text)}catch(error){$('composer-status').textContent=String(error);$('composer-status').className='composer-status error'}finally{this.disabled=false}};
$('composer').onkeydown=function(event){if(event.key==='Enter'&&!event.shiftKey){event.preventDefault();$('send').click()}};$('composer').oninput=function(){this.style.height='auto';this.style.height=Math.min(this.scrollHeight,180)+'px'};
$('new-button').onclick=function(){$('reset-confirm').classList.add('show')};$('reset-no').onclick=function(){$('reset-confirm').classList.remove('show')};$('reset-yes').onclick=function(){$('reset-confirm').classList.remove('show');sendText('/new')};$('mobile-channels').onclick=function(){$('rail').classList.toggle('open')};
function connectEvents(){var source=new EventSource('/api/chat/events');source.addEventListener('message',function(event){appendMessage(JSON.parse(event.data),true)});source.addEventListener('typing',function(event){var data=JSON.parse(event.data);if(data.active)state.typing.add(data.channel_id);else state.typing.delete(data.channel_id);renderTyping()});source.addEventListener('confirmation',function(event){confirmationCard(JSON.parse(event.data))});source.addEventListener('confirmation_resolved',function(event){resolvedConfirmation(JSON.parse(event.data))});source.onerror=function(){$('composer-status').textContent='Reconnecting to local chat…'}}
async function init(){var response=await fetch('/api/chat/channels');state.channels=await response.json();renderChannels();if(state.channels.length)await selectChannel(state.channels[0]);connectEvents()}
init().catch(function(error){$('stream').innerHTML='<div class="empty">'+escapeMarkdownHtml(error)+'</div>'});
</script></body></html>`;
